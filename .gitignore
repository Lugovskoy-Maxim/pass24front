node_modules/
.env
.env.local
*.db
.DS_Store
pass365/

# MongoDB runtime data (Docker bind mount) — never commit collections/WiredTiger files
data/*
!data/mongo/
data/mongo/*
!data/mongo/.gitkeep

# WiredTiger / journal / diagnostic artifacts (if path changes)
*.wt
WiredTiger*
mongod.lock
storage.bson
