# Локальная разработка на Windows — доступ к Mongo с хоста
services:
  mongo:
    ports:
      - "127.0.0.1:27017:27017"
  backend:
    ports:
      - "127.0.0.1:4000:4000"
  frontend:
    ports:
      - "127.0.0.1:3000:3000"